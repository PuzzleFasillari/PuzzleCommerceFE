import { Josefin_Sans } from 'next/font/google';

const inter = Josefin_Sans({
  weight: ['300', '400', '500', '700'],
  style: ['normal', 'italic'],
  subsets: ['latin'],
  display: 'swap',
});
export const fonts = {
  inter,
};
